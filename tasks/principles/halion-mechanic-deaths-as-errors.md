# Фильтры журнала: Все, Ошибки, Напулы

## Когда применять
При проектировании категорий и фильтров журнала RLHelper.

## Правило
Использовать три фильтра: «Все», «Ошибки», «Напулы». Обычные способности доступны в «Все», а применения, отмеченные как нарушения, — также в «Ошибки». Отдельный фильтр «Способности» не нужен для выбранного сценария РЛа.

Показывать панель фильтров только при достаточной высоте окна для минимум пяти строк лога под ней. При уменьшении окна скрывать панель и отдавать освободившееся место логу, сохраняя выбранный фильтр.

Отслеживаемые смерти от механик Халиона (`MECHANIC_DEATH`) включать в категорию «Ошибки». Наличие этих событий само по себе не требует отдельного фильтра «Смерти».

## Обоснование
Для РЛа эти записи показывают ошибку исполнения механики. Отдельная категория смертей дублирует этот сценарий разбора.

## Проверка
- «Ошибки» включают `MECHANIC_DEATH`, даже если сохранённая запись имеет тип `INFO`.
- Правило не распространяется автоматически на любые другие смерти игроков.
- Панель содержит только «Все», «Ошибки», «Напулы».
- Итоги по духам, вязкой гадости и удушливому газу остаются информационными: видны в «Все», отсутствуют в «Ошибки» и не получают красную подложку.
- Попадание вихря по хилеру (`VORTEX_HIT`) — ошибка; промах/иммун по хилеру (`VORTEX_MISSED`) — тоже ошибка.
- Новые смерти от отслеживаемых механик Халиона и попадания/промахи вихря по хилеру записываются как `TACTIC_VIOLATION`; старые записи этих видов с типом `INFO` также видны в «Ошибки».
- Фильтры одинаково работают в текущем бою и сохранённой истории.

## Причина смерти на Халионе

При определении `MECHANIC_DEATH` учитывать исходную ошибку механики, а не только добивающий удар. Попадание лезвий, падение метеорита или огонь от метеорита с последующей смертью считать смертью от этой механики, даже если добила аура Халиона или другой урон. Пользователь подтвердил это правило 2026-09-25: в ХМ урон механик практически не отхиливается.

Проверки:
- Лезвия → аура → `UNIT_DIED` сохраняют причину «лезвия».
- Падение метеорита или его огонь → другой урон → `UNIT_DIED` сохраняют исходную механику.
- Не требовать смертельного удара механикой или положительного overkill.
- При проверке буфера оценивать потерю попаданий до смерти; не объявлять последующий урон сам по себе ложным срабатыванием.

- Резервное подтверждение смерти: после попадания проверять состояние пострадавшего по GUID в рейдовом ростере каждые 0,2 секунды, максимум 3 секунды после последнего попадания механики. Если игрок выжил, отменять проверку; новое попадание запускает новое окно. При смерти, окончании боя или отключении модуля отменять таймеры. `UNIT_DIED` и ростер не должны создавать две записи одной смерти.
